<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
</head>
<body class = "d-flex flex-column min-vh-100">
    <header class="stick-top">
        <nav class="navbar navbar-expand-lg navbar-dark">
            {{!-- container wrapper --}}
            <div class ="container">
                <h1 class="main-title">Tech Blog</h1>
                {{!-- toggle button --}}
                <button class ="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="toggle navigation">
                    <span class ="navbar-toggler-icon"></span>
                </button>
                {{!-- collapse wrapper --}}
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    {{!-- left links --}}
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        {{!-- link to Home --}}
                        <li class="nav-item">
                            <a class="nav-link d-flex flex-column text-center" aria-current="page" href="/">
                            <i class= "fas fa-home fa-lg"></i>
                            <span class="mt-3 small">Home</span>
                            </a>
                            {{!-- link to Dashboard --}}
                        <li class="nav-item">
                            <a class="nav-link d-flex flex-column text-center" aria-current="page" href="/dasboard">
                            <i class="fas fa-comment-dots fa-lg"></i>
                            <span class="mt-3 small">Dashboard</span></a>
                        </li>
                        {{!-- Link to Logout, only show if user is logged in --}}
                        {{#if loggedIn}}
                        <li class="nav-item">
                            <a class="nav-link d-flex flex-column text-center" aria-current="page" href="#" onclick="logout()">
                                <i class="fas fa-sign-in-alt fa-lg"></i>
                                 <span class="mt-3 small">Log out</span></a>
                        </li>
                        {{!-- if not logged in show these links instead --}}
                        {{!-- Link to login --}}
                {{else}}
                <li class="nav-item">
                    <a class="nav-link d-flex flex-column text-center" aria-current="page" href="/login">
                    <i class = "fas fa-sign-in-alt fa-lg"></i>
                     <span class="mt-3 small">Log in</span></a>
                </li>
                {{!-- link to sign up --}}
                <li class="nav-item">
                    <a class="nav-link d-flex flex-column text-center" aria-current="page" href="/signup">
                    <i class = "fas fa-user-plus fa-lg"></i>
                     <span class="mt-3 small">Sign up</span></a>
                </li>
                {{/if}}
                    </ul>
                </div>
                {{!-- end of collapse wrapper --}}
>            </div>
        </nav>
    </header>
    {{!-- fillable body --}}
    <main>
        {{{body}}}
    </main>
    {{!-- footer --}}
    <footer class="mt-auto text-center">
        &copy PC challenge 14 2022
    </footer>
    {{#if loggedIn}}
    {{!-- links --}}
    <script src="/js/logout.js"></script>
    {{/if}}
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
</body>
</html>

